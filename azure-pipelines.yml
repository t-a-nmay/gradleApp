trigger:
- master

pool:
  name: default

steps:
- script: echo my first azure pipeline gradle project
  displayName: 'Run a one line script'
- script: gradle build
  displayName: 'Build'

- script: gradle run
  displayName: 'Run'

